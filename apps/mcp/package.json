{
  "name": "@falsafa/mcp",
  "version": "0.4.0",
  "description": "Open-source MCP server for the Falsafa corpus. Fifteen librarian-flavored tools — eight catalog tools, read_wiki and read_wiki_full for the rule-based wiki layer, and five atlas_* tools over the ontology layer (entities, per-work rosters, citation graph, coverage) — so any LLM (Claude, ChatGPT, Hermes via OpenRouter, etc.) can navigate the catalog without an API key.",
  "license": "MIT",
  "author": "Adnan Abbasi <adnan@thothica.com> (https://meetadnan.com)",
  "homepage": "https://github.com/adoistic/falsafa",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adoistic/falsafa.git",
    "directory": "apps/mcp"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "falsafa-mcp": "./dist/index.js"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --target=node --outdir=./dist --format=esm",
    "dev": "bun run ./src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "eval": "bun run eval/run-evals.ts",
    "eval:case": "bun run eval/run-evals.ts",
    "prepack": "npm run build",
    "prepublishOnly": "node scripts/check-license-drift.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "claude",
    "openai",
    "anthropic",
    "literature",
    "philosophy",
    "translation",
    "rag",
    "karpathy"
  ]
}