---
import Base from "../../layouts/Base.astro";
import ComingSoon from "../../components/ComingSoon.astro";
---

<Base
  title="Numbers"
  description="Catalog size, eval pass rates, cross-link density, ingestion cost. Every number that shows up in the launch writeup, with a deep link to the row that produced it."
>
  <ComingSoon
    kicker="Numbers"
    title="By the numbers"
    blurb="Works, chapters, paragraphs, languages, eras, eval pass rates, cross-link counts, ingestion compute hours, dollar costs. Every claim in the writeup tied back to the row in the dataset that produced it."
  />
</Base>
